### RELATÓRIO TÉCNICO - RADAR DE PREÇOS v5.0 ###
\n1. PROBLEMA ATUAL:
O robô (Node.js) rodando no GitHub Actions está recebendo Erro 403 (Forbidden) ao tentar acessar a API do Mercado Livre.
\n2. ENDPOINT UTILIZADO:
URL: https://api.mercadolibre.com/sites/MLB/search?q={termo}&limit=10&sort=relevance
\n3. CÓDIGO DA REQUISIÇÃO (scanner.js):
async function fetchMercadoLivre() {
  logger.info('Iniciando busca real no Mercado Livre...');
  const products = [];
  const queries = ['iPhone', 'PlayStation 5', 'Notebook', 'Smart TV', 'Air Fryer', 'Samsung Galaxy'];

  for (const q of queries) {
    try {
      const response = await axios.get(CONFIG.mlApi, {
        params: { q, limit: 10, sort: 'relevance' },
        timeout: CONFIG.timeout
      });
--
    const products = await fetchMercadoLivre();
    if (products.length > 0) {
      await fs.ensureDir(path.dirname(CONFIG.dataPath));
      await fs.writeJson(CONFIG.dataPath, products, { spaces: 2 });
      await generateSitemap(products);
      logger.success(`Scanner concluído: ${products.length} produtos publicados.`);
    } else {
      logger.warn('FIM: Nenhum produto novo encontrado.');
    }
  } catch (err) {
    logger.error('Erro crítico no scanner', { error: err.message });
\n4. LOG DE ERRO REAL:
[ERROR] Erro na busca ML: iPhone { error: 'Request failed with status code 403' }
\n5. CAUSA PROVÁVEL:
- Falta de 'Authorization: Bearer ACCESS_TOKEN' no cabeçalho.\n- Bloqueio de IP de data center (GitHub Actions) pelo Mercado Livre.
\n6. O QUE PRECISO:
Preciso de uma solução para o scanner.js que consiga coletar ofertas reais (Título, Preço, Imagem, URL) sem ser bloqueado pelo erro 403. \nConsidere:\n- Uso de Puppeteer/Playwright com stealth mode.\n- Implementação de OAuth2 para obter o Access Token automaticamente.\n- Uso de Proxy ou APIs de scraping (como ScraperAPI).
